function [XTrain, YTrain, XVal, YVal] = prepareDataTrain_DNN(options, data, labels)
switch options.model
    case 'Your model'
    otherwise
        [XTrain, YTrain, XVal, YVal] = splitDataTrain(data, ...
            options.hyperparameters.data.windowSize.value, ...
            options.hyperparameters.data.stepSize.value, ...
            options.hyperparameters.training.ratioTrainVal.value, ...
            options.modelType, options.dataType);
end
end